# Arduino LED

Minimal README to satisfy pyproject `readme` reference.

This package provides a small `ArduinoLEDController` helper to send emotion
strings over a serial connection to an Arduino that drives LEDs.
